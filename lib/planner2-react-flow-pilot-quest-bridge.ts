/**
 * Rezerwa pod integrację z questami: `linkQuests` i podobne akcje z
 * [`board/quests-actions`](../app/(app)/campaign/[id]/board/quests-actions.ts)
 * powinny być wywoływane dopiero po zdefiniowaniu powiązania event ↔ quest / wątek.
 * Pilot React Flow nie wymaga tego do działania (plan: opcjonalny krok).
 */

export {};
